name: Update Radio Data

on:
  schedule:
    - cron: '0 4 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: pip install requests
      
    - name: Run data preprocessing
      run: python scripts/preprocess-data.py
      
    - name: Create Pull Request for Data Update
      uses: peter-evans/create-pull-request@v5
      with:
        commit-message: "🤖 Auto-update radio data"
        title: "🤖 Auto-update radio data"
        body: "自动更新广播电台数据"
        branch: update-radio-data
        delete-branch: true
