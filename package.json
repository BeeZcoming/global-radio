{
  "name": "global-radio",
  "version": "1.0.0",
  "description": "Global Radio Stations Player",
  "type": "module",
  "scripts": {
    "preprocess": "node scripts/preprocess-data.js",
    "split": "node scripts/split-by-region.js",
    "verify": "node scripts/verify-data.js",
    "update": "npm run preprocess && npm run split && npm run verify",
    "build": "echo 'No build step needed for static site'"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}
